~ConsentCompliance(fitbit, user) -< ~ValidConsent(X, Y),  ~GivenConsent(Y, X).
~ValidConsent(X, Y) <- ~ConsentStatusRefused(X, Y).



%Facts
DataSubject(user).
DataController(fitbit).
~ConsentStatusRefused(fitbit, user).