~ConsentCompliance(fitbit, user) -< ~ValidConsent(X, Y),  ~GivenConsent(Y, X).
~ValidConsent(X, Y) <- ~ConsentStatusNotGiven(X, Y).



%Facts
DataSubject(user).
DataController(fitbit).
~ConsentStatusNotGiven(fitbit, user).