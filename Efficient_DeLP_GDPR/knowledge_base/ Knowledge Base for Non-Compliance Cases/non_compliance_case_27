~ConsentCompliance(fitbit, user) -< ~ValidConsent(X, Y), ~ConsentGiven(Y, X).
~ValidConsent(X, Y) <- ~ContextForConsent(X, Y).



%Facts
DataSubject(user).
DataController(fitbit).
~ContextForConsent(fitbit, user).