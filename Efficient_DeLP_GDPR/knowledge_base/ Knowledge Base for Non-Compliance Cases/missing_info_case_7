~ConsentCompliance(fitbit, user) -< ~ValidConsent(X, Y), ~GivenConsent(Y, X).
~ValidConsent(X, Y) <- ~StatusValidForProcessing(X, Y).


%Facts
DataSubject(user).
DataController(fitbit).
~StatusValidForProcessing(fitbit, user).