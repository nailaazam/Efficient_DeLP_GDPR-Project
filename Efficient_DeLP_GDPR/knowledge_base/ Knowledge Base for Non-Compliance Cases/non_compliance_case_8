~ConsentCompliance(fitbit, user) -< ~ValidConsent(X, Y).
~ValidConsent(X, Y) <- ~ConsentStatusExplicitlyGiven(X, Y).


%Facts
DataSubject(user).
DataController(fitbit).
~ConsentStatusExplicitlyGiven(fitbit, user).